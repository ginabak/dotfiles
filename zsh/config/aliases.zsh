# Local aliases
# --------------------------------------

alias gpom="git pull origin master"
alias gpom1="git pull origin main"
alias gfap="git fetch --all --prune"
alias glg="git log --oneline --abbrev-commit"
alias gcf="git commit --fixup"
